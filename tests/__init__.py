"""
Test suite for FastML Serve API.

This package contains unit tests for all FastML Serve components,
including API endpoints, model functionality, and input validation.
Tests use mocked models to ensure fast execution and avoid dependencies
on actual ML model inference.

Run tests with: uv run pytest tests/ -v
"""
